# Скопируйте этот файл в .env и заполните значения

# PostgreSQL — строка подключения из Render Dashboard
DATABASE_URL=postgresql://user:password@host:5432/eduhabit

# JWT — любая длинная случайная строка
JWT_SECRET=your_super_secret_key_here

# Порт (Render подставит автоматически)
PORT=3000
